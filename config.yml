title: HARMONICA COURSE
description: TRY 1 - CREATING HDQ
template:
  repo: harmonica-course-template
  name: harmonica-course
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
